---
# =============================================================================
# STIG ID  : RHEL-10-500720
# Rule ID  : SV-281158r1166426_rule
# Severity : medium
# CCI      : CCI-000018
# Title    : RHEL 10 must generate audit records for all account creations, modifications, disabling, and termination events that affect "/etc/opasswd".
#
# Description:
#   In addition to auditing new user and group accounts, these watches will
#   alert the system administrator(s) to any modifications. Any unexpected
#   users, groups, or modifications should be investigated for legitimacy.
#   Satisfies: SRG-OS-000004-GPOS-00004, SRG-OS-000037-GPOS-00015,
#   SRG-OS-000042-GPOS-00020, SRG-OS-000062-GPOS-00031,
#   SRG-OS-000304-GPOS-00121, SRG-OS-000392-GPOS-00172,
#   SRG-OS-000462-GPOS-00206, SRG-OS-000470-GPOS-00214,
#   SRG-OS-000471-GPOS-00215, SRG-OS-000239-GPOS-00089,
#   SRG-OS-000240-GPOS-00090, SRG-OS-000241-GPOS-00091,
#   SRG-OS-000303-GPOS-00120, SRG-OS-000466-GPOS-00210,
#   SRG-OS-000476-GPOS-00221
#
# Check Text:
#   Verify RHEL 10 generates audit records for all account creations,
#   modifications, disabling, and termination events that affect
#   "/etc/security/opasswd" with the following command:
#
#   $ sudo auditctl -l | egrep '(/etc/security/opasswd)'
#   -a always,exit -F arch=b32 -F path=/etc/security/opasswd -F perm=wa -F
#   key=identity
#   -a always,exit -F arch=b64 -F path=/etc/security/opasswd -F perm=wa -F
#   key=identity
#
#   If the command does not return a line, or the line is commented out, this
#   is a finding.
#
# Fix Text:
#   Configure RHEL 10 to generate audit records for all account creations,
#   modifications, disabling, and termination events that affect
#   "/etc/security/opasswd".
#
#   Add or update the following file system rule to
#   "/etc/audit/rules.d/audit.rules":
#
#   -a always,exit -F arch=b32 -F path=/etc/security/opasswd -F perm=wa -F
#   key=identity
#   -a always,exit -F arch=b64 -F path=(/etc/security/opasswd -F perm=wa -F
#   key=identity
#
#   Restart the audit daemon with the following command for the changes to
#   take effect:
#
#   $ sudo service auditd restart
# =============================================================================

- name: "RHEL-10-500720 | RHEL 10 must generate audit records for all account creations, modifications, disabling, and termination events that affect /etc/opasswd."
  when: rhel_10_500720 | bool
  tags:
    - RHEL-10-500720
    - medium
    - CCI-000018
  block:
    - name: "RHEL-10-500720 | Check audit rule for /etc/security/opasswd"
      ansible.builtin.command:
        cmd: auditctl -l
      register: rhel_10_500720_rules
      changed_when: false
      failed_when: false

    - name: "RHEL-10-500720 | Add audit rule for /etc/security/opasswd (b32)"
      ansible.builtin.lineinfile:
        path: /etc/audit/rules.d/rhel10-stig.rules
        line: "-a always,exit -F arch=b32 -F path=/etc/security/opasswd -F perm=wa -F key=identity"
        create: true
        mode: '0640'
        owner: root
        group: root
      when: "'/etc/security/opasswd' not in rhel_10_500720_rules.stdout"
      notify: restart auditd

    - name: "RHEL-10-500720 | Add audit rule for /etc/security/opasswd (b64)"
      ansible.builtin.lineinfile:
        path: /etc/audit/rules.d/rhel10-stig.rules
        line: "-a always,exit -F arch=b64 -F path=/etc/security/opasswd -F perm=wa -F key=identity"
        create: true
        mode: '0640'
        owner: root
        group: root
      when: "'/etc/security/opasswd' not in rhel_10_500720_rules.stdout"
      notify: restart auditd

    - name: "RHEL-10-500720 | Re-check audit rule for /etc/security/opasswd"
      ansible.builtin.command:
        cmd: auditctl -l
      register: rhel_10_500720_verify
      changed_when: false
      failed_when: false

    - name: "RHEL-10-500720 | Assert audit rule for /etc/security/opasswd is present"
      ansible.builtin.assert:
        that: "'/etc/security/opasswd' in rhel_10_500720_verify.stdout"
        fail_msg: "result=FAIL stig_id=RHEL-10-500720 detail=Audit rule for /etc/security/opasswd (key=identity) is missing."
        success_msg: "result=PASS stig_id=RHEL-10-500720 detail=Audit rule for /etc/security/opasswd (key=identity) is present."

  rescue:
    - name: "RHEL-10-500720 | RESCUE - record failure and continue"
      ansible.builtin.debug:
        msg: "result=ERROR stig_id=RHEL-10-500720 detail=Task failed unexpectedly — see prior error for details."
